import { DimHints } from "../domain/types";

export async function ensureDims(_hints: DimHints): Promise<void> {
  // Placeholder: would upsert dimensions in Power BI
  return;
}

export default ensureDims;


