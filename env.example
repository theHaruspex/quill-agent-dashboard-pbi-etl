# Runtime
NODE_ENV=development
LOG_LEVEL=info
PORT=3000

# Power BI (Service Principal)
POWERBI_TENANT_ID=
POWERBI_CLIENT_ID=
POWERBI_CLIENT_SECRET=

# Power BI Target
POWERBI_WORKSPACE_ID=
POWERBI_DATASET_ID=

# DynamoDB (Idempotency Ledger)
DYNAMO_TABLE_NAME=
DYNAMO_TTL_DAYS=14

# Aloware (Roster/Config)
ALOWARE_RING_GROUP_ID=8465

# HubSpot (Adapters/Enrichment)
# Prefer HUBSPOT_PRIVATE_APP_TOKEN (Private App Token). HUBSPOT_CLIENT_SECRET is a fallback.
HUBSPOT_PRIVATE_APP_TOKEN=
HUBSPOT_CLIENT_SECRET=

# -------------------------------------------------------------------
# Notes
# 1) Copy this file to .env and fill values:
#    cp env.example .env
# 2) The app auto-loads .env (dotenv) in local/server and lambda entrypoints.
# 3) Only outbound Aloware events are counted (per project requirement).
# 4) Schemas and table names are defined in docs/; the service uses those.


